# Changelog

## [1.11.1] - 2026-09-14

### Changed

- **确立「脚本管理器无关」的实现原则并落实**：凡由宿主实现的 GM 接口都必须有兜底。样式注入改为优先 `GM_addStyle`、宿主未实现时退回原生 `<style>` 标签注入（含 `document-start` 阶段 `head` 尚未解析时的时序处理）；诊断信息中补充脚本管理器标识（`GM_info.scriptHandler`），便于区分不同管理器下的问题。
- 依据：脚本不应绑定任何单一管理器——Tampermonkey 是多数人的选择，ScriptCat 与其它管理器也各有用户。

## [1.11.0] - 2026-09-14

### Fixed

- **修复 ScriptCat 下 QwenWork 统计成倍膨胀的问题**：本地存储此前没有内存缓存，`DataStore.get()` 每次都直接读 GM 存储。Tampermonkey 的 `GM_setValue / GM_getValue` 是同步的，写后读立即可见；而 **ScriptCat 的数据操作是异步的**（写入经 IPC 落到 IndexedDB，官方文档明确提示写入后立即关闭页面可能丢失更新），同步的 `GM_getValue` 返回的缓存副本可能滞后于刚才的写入。一次采集中连续多次「读 → 合并 → 写」时，后面的合并会读到前面的合并之前的数据，同一批记录被当成新记录反复追加——这就是千问统计翻倍的根因，且只在 ScriptCat 上发生。现改为**同一页面会话内只读一次 GM 存储、后续读写走内存副本**，对所有脚本管理器行为一致。
  - 已知取舍：同一平台的两个标签页各自持有内存副本，后写入的会覆盖先写入的。
  - 修复后实测：清空重采 84 条 / 0 重复，09-13 = 100/5 条、09-14 = 19/2 条，二次加载无新增。

### Note

- 本版本起明确支持 Tampermonkey 与 ScriptCat 两个脚本管理器。脚本仅使用标准 GM API（`GM_getValue` / `GM_setValue` / `GM_addStyle`），未依赖任何 Tampermonkey 专有行为。

## [1.10.4] - 2026-09-14

### Added

- **新增「复制诊断信息」入口**（面板页脚，与「重置本页数据」并列）：把本地存储的按日聚合与全部记录的内容明细复制到剪贴板。数据存在油猴（Tampermonkey）的存储里，页面控制台读不到，此前排查数据问题只能让用户手动翻存储；这个入口把「报错所需的数据」变成一次点击。

## [1.10.3] - 2026-09-14

### Added

- **新增「重置本页数据」入口**（面板页脚）：本地已采集的用量数据全部可以由页面重建，因此重置是无损操作。点击并确认后清空当前平台的本地记录并自动刷新重新采集。用于数据异常时的自救，无需打开脚本管理器手动清存储。

### Note

- 针对「QwenWork 统计偏大」的后续排查结论：v1.10.2 的存量迁移在该用户的存储上未发生任何收敛（前后数字完全一致），说明其重复记录在字段上并不完全相同（v1.10.2 只能收敛「时间＋来源＋详情＋金额」完全一致的重复）。已排除详情列按视口宽度截断的可能（1440px / 980px / 还原三档实测解析结果一致）。此类污染无法在不接触用户数据的前提下进一步定位，故提供重置入口作为自救手段；若重置后再次出现偏差，请通过页脚「重置本页数据」前的控制台导出存储内容以便定位。

## [1.10.2] - 2026-09-14

### Fixed

- **修复 QwenWork 统计偏大的问题**：会话 ID 此前在「时间 + 来源 + 详情 + 金额」之外还追加了出现序号（`|1`、`|2`…），而序号取决于当次解析的内容构成——页面新增记录后序号会漂移，同一条记录就会拿到新 ID 被当成新记录重复入库。现改为纯内容 ID，并新增存量迁移：按记录自身字段重算标准 ID、按 ID 去重，把历史重复自动收敛掉（实测注入 3 条重复后运行一次，存储即恢复无重复状态）。
  - 取舍：同一分钟内来源、详情、金额完全相同的两笔会合并为一条。这种情况罕见，且页面 DOM 的重复行与真实重复无法区分，宁可少计不可多计。
- **修复 QwenWork 的积分历史总是停留在中途页码的问题**：采集循环在「某页无新增」时中断，页面就停留在那一页（通常是第 2 页），下次进入看到的不是最新记录。现在采集前先退回第 1 页、首页无新增则跳过翻页、采集完再把分页退回第 1 页。

## [1.10.1] - 2026-09-13

### Fixed

- **修复 SPA 切换页面后面板仍然残留的问题**：面板的显示条件此前只在脚本启动时判定一次。WorkBuddy 这类单页应用在站内切换到「API 管理」等其它页面后，路由已变但脚本仍认为自己在用量页，于是把被移除的面板重建到了兜底位置（页面最顶端、盖住站点头部）。现在改为**实时判断**——每次渲染与 DOM 变化都重新读取当前的 `pathname` / `hash`：不在用量页时移除面板且不重建，回到用量页时自动恢复。数据采集不受影响，仍在后台继续。
- 各平台的显示条件由适配表声明（`pagePath` 前缀 + 可选的 `pageHash`）：Trae 的各 Tab 通过 hash 切换，离开用量 Tab 即隐藏，与页面行为一致。

## [1.10.0] - 2026-09-13

### Changed

- **面板位置从页面底部移到内容区顶部**，三个平台统一。插入点选在「页面自身的滚动容器内部」，因此面板与页面内容共用同一条滚动条，不再出现两套滚动；面板自身不设 `overflow`，不会产生内层滚动条。
  - 面板宽度与页面内容列对齐：Trae 701px、QwenWork 800px、WorkBuddy 738px，左边界与页面卡片完全一致（QwenWork 的 800px 约束是从页面内容列动态读取的，不写死）。
  - 页面是 SPA，理想插入点常在首帧之后才渲染出来，因此挂载改为「懒校正」：落位之前每次 DOM 变化与渲染都校正一次，落位后即停止检查。
- **样式重构为一套组件规范 + 三张主题变量表**。面板内不再写死任何颜色，全部改用 CSS 变量（`--tee-bg` / `--tee-surface` / `--tee-track` / `--tee-border` / `--tee-text*` / `--tee-accent` / `--tee-bar-*` / `--tee-btn-*` / `--tee-radius*` / `--tee-font` / `--tee-value-size`），三个平台各给一张变量表，组件代码完全共用。
- **主题取值全部来自各页面自身的实测 token**，而不是凭感觉配色：

  | | Trae | QwenWork | WorkBuddy |
  | --- | --- | --- | --- |
  | 面板底色 | `rgba(224,226,242,.04)` | `#FFFFFF` | `#FFFFFF` |
  | 内层卡片 | `rgba(224,226,242,.06)` | `rgba(20,20,20,.04)` | `#F5F7FA` |
  | 描边 | `rgba(224,226,242,.12)` | `rgba(20,20,20,.10)` | `rgba(0,0,0,.08)` |
  | 圆角（面板／内层） | 6px / 6px | 16px / 12px | 24px / 16px |
  | 强调色 | `#32F08C` | `#5B4DFF` | `#00836A` |
  | 用量条渐变 | `#1FB86A → #32F08C` | `#8D82FF → #5B4DFF` | `#8DB8AA → #00C29A` |
  | 主按钮 | 亮绿底＋近黑字 | 4% 黑胶囊 | 深色胶囊 |
  | 字体栈／数值字号 | SF Pro 系统栈 / 22px | system-ui 栈 / 22px | 系统栈 / 24px |

- **补齐交互状态**：用量行与下级明细行增加 hover 底色；折叠标题增加 hover 与 `:focus-visible` 焦点环；刷新按钮增加 hover / active / `:focus-visible`。所有可聚焦元素都有了键盘可见的焦点样式。
- **补齐可访问性与多端适配**：数值与表格类文本启用 `font-variant-numeric: tabular-nums`（数字等宽、纵向对齐）；窄屏（≤900px）下收缩列宽与字号，避免模型名被挤成空白；尊重系统「减少动态效果」设置，开启后关闭全部过渡动画。
- 更新 README 的效果预览为三个平台的实拍截图（`docs/images/Trae.png` / `QwenWork.png` / `WorkBuddy.png`），并同步「支持平台」表的面板主题说明与「使用」一节的面板位置描述。

### Fixed

- 修复 QwenWork 上面板把页面「小节标题与其卡片」从中间拆开的问题（原先插在第一张卡片之前，「订阅套餐」标题会孤零零留在面板上方）。

### Note

- WorkBuddy 的强调色在数值文字上使用 `#00836A` 而非品牌色 `#00C29A`：后者在白底上的对比度不足以支撑正文级可读性。用量条等非文字元素仍使用品牌色，兼顾识别度与可读性。

## [1.9.5] - 2026-09-13

### Fixed

- **修复面板间距被目标站点样式清零的问题**：目标页面加载的样式表里存在带 `!important` 的重置规则，会把注入面板的 `margin` 与 `padding`（连内联样式里的也一并）清零。实测确认只有同为 `!important` 的声明才能夺回控制权（同一属性加 `!important` 后由 `0px` 变为 `30px`，不加则无效）。现面板内全部间距声明改为 `!important`，选择器一律限定在 `#trae-enhancer-root` 内，不会影响页面自身。此前卡片的 `padding`、分区的 `margin-bottom`、根容器的左右留白都处于被清零状态。
- 修复「积分消耗趋势」标题与其上方数值标签的文字重叠：数值标签绝对定位在柱顶上方，依赖容器顶部留白避让，而该留白此前被清零，于是最长的柱子把标签顶到了标题上。现恢复留白并把净空从 20px 增至 24px（实测标题底部与标签顶部间距 17px）。
- 修复用量为 0 的模型／使用端仍渲染一段保底用量条的问题。现在用量为 0 时该行整条留空，不再渲染柱体，避免看起来像有消耗。

## [1.9.4] - 2026-09-13

### Fixed

- **从「各使用端积分消耗」中移除「未知使用端」这一占位行**：它不是真实存在的使用端，桶里的记录都带模型名、已按真实模型在「各模型积分消耗」中各自成行；再以使用端身份列一次，会让人以为同一笔积分被计算了两遍。同时在那一节补了一句说明，告知被摘出的积分与请求数，避免该节静默地加不出总数。

### Changed

- Trae 的模型名占位文案改为 **「auto（未标注）」**，与页面在未指定模型时显示 `auto` 的行为对齐。占位文案改由平台适配表声明（`modelPlaceholder`），其他平台仍使用「（未标注）」。

## [1.9.3] - 2026-09-13

### Changed

- **撤销使用端桶的下级细分**：WorkBuddy 的「未知使用端」此前可展开查看它包含哪些模型，但那些模型本来就在「各模型积分消耗」中各自成行，再在别处叠一层折叠属于重复呈现。现在的原则是——模型归属正确之后，每个模型都已有了自己的位置，就不需要再收起来。
- 仅保留「模型名未标注」这一类的可展开下级（模型桶本身就是模型维度，只能按记录详情细分）。QwenWork 的 `—` 行因此保留折叠，因为它的下级是「每日积分过期」「透支扣减」这类**并非模型**的条目，在别处没有对应位置。
- Trae 的模型列表保持扁平：v1.9.2 修正归属后，各模型已独立成行，无需折叠。

## [1.9.2] - 2026-09-13

### Fixed

- **修复 Trae 的模型归属错误**：一次对话可能横跨多个模型，此前面板按会话级 `model_name` 整笔归属，导致同一次对话里的其它模型被完全隐藏。实测 37 条会话中有 7 条各含 2 个模型，被隐藏的 `Seedream-5.0-lite` 单独就占总量 32%（96.22 / 301.46）。现优先按接口 `usage_group_details` 中每个模型各自的消耗归属；该字段不存在时（QwenWork、WorkBuddy）仍回退到会话级模型名。
  - 多模型会话的 group 金额之和与会话金额完全一致，因此总数与趋势不受影响（修正后三行之和仍为 301.46）。
  - 归属修正后的模型行：DeepSeek-V4-Flash 正式版 146.94 / Seedream-5.0-lite 96.22 / GLM-5.3 58.30。

## [1.9.1] - 2026-09-13

### Changed

- **不再改写接口采集到的模型标识**：此前把模型名为空或为 `—` / `-` / `--` 的记录统一显示为「过期积分」，但那些值本身就是页面给出的「未公布」标记，改名会让人误以为检测到了积分过期。现在原样展示采集到的内容（`-` 就显示为 `-`），只有接口完全没给值时才使用「（未标注）」占位。折叠细分对这类条目依然生效。
- 移除本地原始响应体留存（`api_responses`）。该数据只有写入、没有任何读取方，属于纯调试残留，却把整份接口响应连同用户内容复制进本地存储。移除后存储体积明显下降。

### Security

- Trae 的明细接口会返回 `user_input_preview`（用户输入的预览片段）。该字段面板从不展示，现于归一化阶段丢弃，不再写入本地存储。

### Fixed

- 修复折叠控件在无有效下级维度时的冗余渲染：当某个桶的下级只有脚本自己造的占位名（例如 Trae 没有可用的次级字段）时不再渲染折叠控件，避免出现只能展开出一项「（无详情）」的无意义交互。

### Verified

- Trae 首次完成真机验证：拦截器（fetch 与 XHR）命中，37 条会话、合计 301.46 积分，总积分与各模型之和一致，分页正常。
- 同时确认 Trae 的会话对象不含 `session_name`，因此模型桶的下级维度对该平台不可用。

## [1.9.0] - 2026-09-13

### Added

- **未分类聚合条目支持展开细分**：面板里「未知使用端」「过期积分」这类把多种来源合并成一行的条目，现在可以展开查看消耗构成，默认收起，避免信息过载。
  - 使用端为空的记录（WorkBuddy 的「未知使用端」）按**模型**细分，能直接看出这些消耗由哪些模型产生。
  - 模型名缺失的记录（QwenWork 的「来源」为 `—`、Trae 的未命名模型，面板中显示为「过期积分」）本身就是模型维度，按模型再分只会得到一堆 `—`，因此改为按记录里的**详情**（QwenWork 的「详情」/ Trae 的「会话名称」）细分。展开标题会写明使用的是哪个维度。
  - 只要有构成项就渲染折叠控件——即便只有一项，也能回答「这一行的数字由什么组成」；仅当唯一子项与父条目同名时才省略，因为那时展开没有新信息。
  - 展开状态按「平台 + 维度 + 条目」记录，面板重绘或数据刷新后自动恢复，不需要重新点开。

## [1.8.1] - 2026-09-13

### Fixed

- **修复 QwenWork 面板数据全为 0 的问题**：1.8.0 把采集流程重构成只执行一次的 `postFetch`，丢掉了 1.7.0 在点击「已使用」失败后等待 3 秒再提取的缓冲，导致「已使用」入口比脚本晚出现时提取被整段跳过，且没有重试、不会自愈。现改为轮询等待入口出现（最长 15 秒）、等待记录渲染出来（最长 15 秒），并在取不到数据时每 5 秒重试、最多 6 次。
- 修复 QwenWork 时间解析的一处兼容性：日期与时间被拆成两行渲染时无法识别，现同时支持单行与双行两种形式，日期分隔符也兼容 `/`。以上均为严格增量，只在原有严格路径取不到时才生效。
- 修复 QwenWork 金额读取过于依赖 `parseFloat` 整行解析的问题，取不到时回退为「读取行内第一个带符号数字」；当「已使用」区域内的金额全部为正数时按正数处理（仅在一条负数都没有时启用）。
- 修复近 7 天没有消耗记录时趋势图整块空白、容易被误认为脚本故障的问题。现退化为展示「最近有消耗的若干天」，并在图下标注真实日期区间与「近 7 天无记录」的原因。
- 采集失败时输出结构化诊断信息（匹配到的日期行数、数据区文本长度，只记数量不记内容），便于定位页面结构变化。

## [1.8.0] - 2026-09-13

### Added

- WorkBuddy（`www.workbuddy.cn/profile/plans-usage`）支持：拦截 `/billing/meter/get-user-request-usage` 获取积分消耗明细，首次进入页面时主动请求最近 30 天的数据，并按 `pageNum` 自动翻页。
- 新增「使用端分布」一节：WorkBuddy 的用量接口按使用端区分（WorkBuddy / CLI 等），面板单独呈现。由于 WorkBuddy 与 CodeBuddy 共用同一账号积分池，该口径为账号级总消耗。
- WorkBuddy 面板浅色主题（`workbuddy-theme`），配色对齐官网控制台（底色 `#F5F7FA`、强调色 `#00C29A`）。
- 平台分发重构为适配表：识别规则、存储键、主题、接口匹配、响应解析、分页构造、首次采集方式全部收敛到一处，后续新增平台只需追加一项。
- 新增「已购权益额度」卡片，用于承载此前被误当作总消耗的权益包金额。

### Fixed

- **修复 QwenWork 数据不落盘**：此前只写入内存变量，刷新页面即丢失，与 README 的持久化声明矛盾。现写入 `qwenwork_usage_data`。
- **修复安装与更新链接失效**：脚本元数据与 README 中的仓库地址此前写成 `asdfz2/trae-dashboard-enhancer`，与真实仓库 `asdfz2/Trae_Qwen-dashboard-enhancer` 不符，导致安装链接 404、已装用户收不到更新。
- 修复自动翻页的当前页判断：此前只读响应中的 `page` 字段，而真实分页字段是 `page_num`，导致永远认为当前在第 1 页、重复拉取全部页。现优先读取请求体中的分页参数。
- 修复翻页状态被持久化的问题：`_autoFetching` / `_pagingKey` 此前写入 GM 存储，翻页途中关闭标签页会使 `finally` 不执行、标志位永久为真，自动翻页从此静默失效。现改为纯内存状态。
- 修复「总积分消耗」可能显示成权益包金额的问题：会话为空时会用 `user_entitlement_pack_list` 的合计顶替总消耗，而那是「已购买」不是「已消耗」。
- 修复模型名兜底逻辑与 CHANGELOG 描述不符：`||` 会先把空模型名替换掉，导致「空模型名显示为过期积分」的分支永不生效。
- 修复金额未做数值转换的隐患：若接口返回字符串型金额，总数会变成字符串拼接而日汇总是数值运算，两者口径会分裂。现统一在读取时转换。
- 修复时间解析：`"YYYY-MM-DD HH:mm:ss"` 形式在部分环境解析不稳定，现统一转成 ISO 风格再解析。
- 修复 QwenWork 时间正则宽严不一：解析侧要求空格分隔且不含秒，容器嗅探侧却允许带秒，会出现「找到容器但解析出 0 条」。现两侧共用同一个容错正则。
- 修复 QwenWork 去重键缺少金额与序号，导致同一分钟、同来源、同详情的多笔消耗被合并漏计。
- 修复定时重绘打断文本选中的问题：改为按数据指纹判断，仅在数据变化时重绘。
- 修复刷新按钮使用内联 `onclick` 的 CSP 兼容风险，改为事件监听。
- 样式注入失败不再中断整个脚本，仅降级为无样式面板。

### Changed

- 移除调试残留：不再注入 `#trae-enhancer-debug` 标记元素；不再把鉴权 token 打印到控制台，同时移除已失效的 `GetUserToken` 调用，主动采集改为复用页面真实请求的参数与请求头；`_paginationLogged` 不再落盘。
- 日志统一由一个受 `DEBUG` 常量控制的出口输出，默认静默。
- 面板版本号改为读取脚本内的 `VERSION` 常量，不再与 `@version` 双写。
- `@grant` 移除已废弃的 `GM_log`，新增 `@updateURL`。
- 脚本名称与描述更新为三个平台。

### Security

- WorkBuddy 的用量接口会返回对话提示词原文（`input` / `inputTrunc`）。脚本只提取统计字段，明确丢弃这些字段且不写入本地存储，该平台也不保存原始接口响应。

## [1.7.0] - 2026-08-20

### Added

- QwenWork DOM 多页翻页：自动检测翻页控件（Ant Design/Element UI/自定义），遍历所有页提取积分消耗记录。
- 过期积分标识：模型名称为空、"—"、"-" 或 "--" 时统一显示为"过期积分"。

### Fixed

- 修复 Tampermonkey 沙箱中 `new MouseEvent` 构造失败导致脚本中断的问题，改用原生 `element.click()`。

### Changed

- 代码优化：删除未使用的 API_CONFIG 字段、死函数、死 CSS 类；简化 `isTraeApi` 为数组 `some()`；清理冗余注释，总代码量减少约 13%。

## [1.6.2] - 2026-08-16

### Added

- QwenWork 面板浅色主题适配：白色背景、蓝色数值、灰色标签，与 qwenwork.cn 白色页面融为一体。

### Fixed

- QwenWork 面板改为全宽显示（max-width 1400px），放在页面最下方，与 Trae 面板布局一致。

## [1.6.1] - 2026-08-16

### Added

- QwenWork 用量数据 DOM 提取：从"已使用"按钮的祖先容器中解析积分消耗记录（时间、来源、详情、积分变更）。

### Fixed

- 只提取"已使用"区域的消耗记录，排除"已获得"数据。
- 修复 `innerText` 在 SVG 元素上为 `undefined` 导致 `trim()` 报错的问题。

### Changed

- 面板容器统一逻辑：qwenwork.cn 直接插入 body 全宽显示，Trae 保持原有容器查找逻辑。

## [1.6.0] - 2026-08-16

### Added

- QwenWork（qwenwork.cn）用量页面支持，自动检测域名并适配。
- QwenWork API 响应适配：`success: true` + `data.usage_records` 字段解析。
- `@match` 规则新增 `https://qwenwork.cn/app/settings/usage*` 和 `https://www.qwenwork.cn/app/settings/usage*`。

### Changed

- `@run-at` 改为 `document-start`，更早安装拦截器。
- 脚本名称改为 "Trae/QwenWork 用量仪表盘增强"。

## [1.5.9] - 2026-08-15

### Fixed

- 修复切换 7天/30天 等范围时另一个范围未完整采集的问题：翻页触发此前依赖单一全局 `_lastApiRequest` 且仅当无翻页进行中才触发，若在 7 天翻页进行中切到 30 天，30 天响应被合并但其翻页被占位漏掉。现每个 API 响应携带自身请求体/请求头按正确范围翻页，并在请求体不同（范围不同）时允许新翻页接管，保证各范围都能完整拉取全部页。

### Known limitation（首次使用需手动操作）

- 脚本只有抓到「30 天」范围的用量请求后才会自动翻页。首次使用请先点击用量页上的「30 天」按钮，再手动往下翻两页，脚本即可自动采集完整数据并持久化到本地。
- 详见 README「使用说明（已知限制）」。

## [1.5.8] - 2026-08-15

### Fixed

- 修复翻页仍停在 2 页的问题：本地 GM 存储跨刷新保留，之前已存过第 1-2 页，刷新后循环从第 2 页开始、其会话已存在，去重守卫误判「无新增」而提前中断。现当接口已给出明确总页数（`total`）时，不再用去重守卫截断，直接拉取全部页；去重守卫仅保留给「无元数据自发现」模式。

## [1.5.7] - 2026-08-15

### Fixed

- 修复自动翻页只爬到第 2 页就停止的问题：翻页回放此前走包装后的 `window.fetch`，其拦截器在 `then` 里已先合并该页数据，导致循环内去重守卫误判为「无新增」而提前中断。现翻页回放改用原始 fetch（`_rawFetch`），每页数据仅由循环显式合并一次，去重守卫可正确判断，最终拉取全部页。

## [1.5.6] - 2026-08-15

### Fixed

- 修复自动翻页始终返回第一页的问题：该用量接口真实分页字段为 `page_num`（请求体例：`{"start_time":…,"end_time":…,"page_size":20,"page_num":1,"usage_type":[7]}`），此前脚本用 `page` 字段翻页，服务端忽略导致每页都拿到第一页。现按请求体实际字段 `page_num` 递增，并保留 `start_time`/`end_time`/`usage_type` 等参数。

## [1.5.5] - 2026-08-15

### Fixed

- 修复自动翻页回放返回 401 的问题：回放请求此前只带 `Content-Type`，丢失了原请求的鉴权头（如 `Authorization`）；现 fetch 与 XHR 均捕获请求头，翻页回放时原样转发（排除 `content-length`/`host`/`connection`/`accept-encoding`）。

## [1.5.3] - 2026-08-15

### Fixed

- 修复首次加载只统计到最近两天数据的问题：fetch 拦截器此前未记录用量请求体，导致自动翻页回放时请求体为空、服务端始终返回第一页；现 fetch 与 XHR 均同步捕获请求体。
- 翻页去重守卫：某页未新增任何会话即停止，避免服务端未按 `page` 分页时无限请求。
- 翻页状态 `_autoFetching` 在结束后复位，允许后续响应再次触发。
- 无分页元数据时启用自发现翻页（上限由 `CONFIG.maxAutoPages` 控制，默认 100 页）。

## [1.5.2] - 2026-08-15

### Fixed

- 按「分」（×100 整数）累加每日积分，今日 / 近 7 天 / 本月与趋势柱均从同一份整数日汇总派生，消除浮点累加导致的「今日与趋势柱」「近 7 天与各柱之和」不一致（约 0.02 的误差）。

## [1.5.1] - 2026-08-15

### Fixed

- 统一「今日」「近 7 天」「本月」与趋势图的统计口径：均按本地自然日从同一份日汇总派生，修复「今日积分消耗」与「积分消耗趋势（近 7 天）」不一致。
- 近 7 天改为含今天在内的连续 7 个本地自然日；去掉 `new Date('YYYY-MM-DD')` / `toISOString` 带来的 UTC 日期偏移。

### Changed

- 重写 README：修复安装链接，补充功能、原理、项目结构与 FAQ。
- 整理仓库结构：截图统一放入 `docs/images/`。
- 新增 `LICENSE`、`CHANGELOG.md`、`docs/ARCHITECTURE.md`。
- 修正脚本元数据：名称、作者、license、homepage、supportURL。

## [1.5.0] - 2026-08-13

### Added

- Trae 用量仪表盘增强面板 v1.5：多维积分统计、模型 breakdown、近 7 天趋势。
- 自动翻页获取全量历史数据。
- 自动重试与 SPA 适配。
